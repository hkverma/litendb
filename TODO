1. Change plan to include slice with dimensions and aggregates. Complete _0 notebook with the example.

Target #1
Create demo with timing

Target #2


1. AddTable - Add type of table dimension or fact
2. Select between all TXXX name or Lxxx names. Make it consistent across python and cpp
3. Clean and write all the notebooks for 
4. Write cache into SSD or storage for persistence
5. In TTable.h remove multiple maps_ copies, keep only one. Do an analysis.
6. Use ctest to run tests (from cmake)
7. What to do if same table is read again & again
8. 